name: better_guitar_tuner
description: Flutter UI shell for Better-Guitar-Tuner.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/tuning/tuning_presets.json
